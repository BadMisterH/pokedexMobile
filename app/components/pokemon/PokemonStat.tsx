import { Row } from "../Row";
import { StyleSheet, View, type ViewProps } from "react-native";

export function PokemonStat({style, ...rest} : Props) {
    return <Row>

    </Row>
}

const styles = StyleShee